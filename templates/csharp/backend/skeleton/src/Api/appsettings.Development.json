{
  // 開発環境向けのログ出力設定
  "Logging": {
    // 出力レベルの設定
    "LogLevel": {
      // 既定のログレベル（開発中は詳細めに出力）
      "Default": "Debug",
      // ASP.NET Core 内部のログレベル
      "Microsoft.AspNetCore": "Information"
    }
  }
}
