# @k1s0-ts-auth/react

`@k1s0-ts-auth/core` の React 連携パッケージです。

`AuthProvider`、`useAuth()`、`useCurrentUser()`、`usePermission()`、`RequireAuth`、`RequirePermission` を提供します。

フロント側の権限判定は表示制御用です。API の最終認可はバックエンドで実施してください。
