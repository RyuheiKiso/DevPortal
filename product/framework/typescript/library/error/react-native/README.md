# @k1s0-ts-error/react-native

React Native bindings for `@k1s0-ts-error/core`.

The package exposes the same Provider, hooks, and ErrorBoundary pattern as the React package, plus `registerNativeGlobalErrorHandler` for forwarding unhandled native errors into the normalized error pipeline.
