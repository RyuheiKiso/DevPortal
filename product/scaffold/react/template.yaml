# Backstage Software Template - React 19 SPA (Vite) スターター
# Backstage UI の /create/templates に表示され、社内利用者が新規 React プロジェクトをここから生成できる

# Backstage Scaffolder のスキーマバージョン (v1beta3 が現行安定版)
apiVersion: scaffolder.backstage.io/v1beta3
# エンティティ種別 (Template = Scaffolder の雛形)
kind: Template
# エンティティ自身を識別するメタデータ
metadata:
  # Template の一意な名前 (kebab-case 必須)
  name: k1s0-scaffold-react-template
  # UI カードの見出し
  title: "React 19 SPA (Vite)"
  # UI カードの説明文
  description: "React 19 + TypeScript + Vite による Web SPA スターターを新規生成"
  # 検索・フィルタ用タグ
  tags:
    - scaffold
    - react
    - vite
    - typescript
# Template 固有の仕様
spec:
  # 所有者 (framework/typescript で先に登録済みの Group)
  owner: k1s0-framework-team
  # 生成される Component の種別
  type: website
  # ユーザー入力フォーム定義 (JSON Schema 形式)
  parameters:
    # 入力グループ (UI 上のステップ)
    - title: プロジェクト基本情報
      # 入力必須項目の列挙
      required:
        - name
      # フィールド定義
      properties:
        # プロジェクト名 (必須)
        name:
          title: プロジェクト名
          type: string
          description: 新規プロジェクトのディレクトリ名 (kebab-case 推奨)
          # kebab-case を強制する正規表現
          pattern: '^[a-z][a-z0-9-]*$'
          # 開いたらこのフィールドにフォーカスする
          ui:autofocus: true
        # 説明文 (任意)
        description:
          title: 説明 (任意)
          type: string
          description: 新規プロジェクトの簡単な説明
          # 未入力時のデフォルト
          default: "React 19 + TypeScript + Vite SPA"
  # 生成時に走らせるアクション (上から順に実行)
  steps:
    # skeleton 配下を取得し Nunjucks プレースホルダを展開する
    - id: fetch
      name: スターターファイルを取得
      action: fetch:template
      input:
        # template.yaml の位置からの相対パス (../skeleton/react を fetch:template の対象とする)
        url: ../skeleton/react
        # 出力先 (Backstage Scaffolder のワークスペース直下)
        targetPath: ./
        # Nunjucks 展開時に参照される変数 (skeleton 内で ${{ values.x }} として読まれる)
        values:
          # プロジェクト名 (package.json の name / index.html の title 等で展開される)
          name: ${{ parameters.name }}
          # 説明文 (README に展開される)
          description: ${{ parameters.description }}
  # 生成後に UI に表示する案内
  output:
    text:
      - title: 完了案内
        content: |
          React 19 SPA スターター ${{ parameters.name }} を生成しました。

          ## 次の手順

          1. `npm install` を実行して依存を解決
          2. `npm run dev` で開発サーバ起動

          詳細は同梱の README.md を参照してください。
